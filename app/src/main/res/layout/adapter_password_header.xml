<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_root_adapter_profile_header"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="4dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp" >

    <TextView
        android:id="@+id/text_header_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="@string/dummy_text"
        android:textSize="@dimen/text_size_small" />

    <ImageView
        android:id="@+id/divide_header_password"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/text_header_password"
        android:layout_marginTop="4dp"
        android:contentDescription="@string/dummy_text"
        android:paddingBottom="4dp"
        android:scaleType="fitXY"
        android:src="@android:color/holo_blue_bright" />

</RelativeLayout>
